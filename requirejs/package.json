{
  "name": "requirejs-try",
  "version": "0.0.1",
  "description": "Try to put requirejs and angularjs together.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Li Luo",
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.7.9",
    "browser-sync": "^2.13.0",
    "gulp": "^3.9.1"
  },
  "dependencies": {},
  "scripts": {
	"test": "browser-sync start --server --startPath app"
  }
}
